apiVersion: v1
kind: Service
metadata:
  name: tenant-policy-gateway
  namespace: tenant-gateway
  labels:
    app.kubernetes.io/name: tenant-policy-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tenant-policy-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
