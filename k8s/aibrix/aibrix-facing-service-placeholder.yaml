apiVersion: v1
kind: Service
metadata:
  name: aibrix-gateway
  namespace: aibrix-system
  labels:
    app.kubernetes.io/name: aibrix-gateway-placeholder
  annotations:
    warning: "Placeholder only. Replace with the real AIBrix Gateway Service."
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aibrix-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8000
